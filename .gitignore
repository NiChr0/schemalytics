# Test artifacts
schema.json
plan.yaml
test_project/
e2e_project/
northwind.sql

# Generated dbt output — any folder name ending in _dbt or _project
dbt_project/
*_dbt/
*_project/

# Python
__pycache__/
*.pyc
*.pyo
*.egg-info/
dist/
build/

# Virtual environments
venv/
.venv/
schemalytics/bin/
schemalytics/lib/
schemalytics/include/
schemalytics/pyvenv.cfg

# IDE
.vscode/
.idea/
*.swp

# Evaluation audit files (local only)
evaluation_audit*.md

# Fine-tune — large binary artifacts (do not commit)
finetune/cuda/*.gguf
finetune/cuda/*.bin
finetune/cuda/adapters-*/
finetune/cuda/schemalytics-agent3-qwen3.5-4b/
finetune/cuda/merged-*/
unsloth_compiled_cache/

# Fine-tune — intermediate labeled/extracted data (reproducible via scripts)
finetune/labeled/
finetune/labeled_batched/
finetune/labeled_complex/
finetune/labeled_spider/
finetune/extracted_complex/
finetune/extracted_spider/

# Fine-tune — training logs
finetune/cuda/*.log

# Fine-tune — raw Spider dataset (third-party, download on demand)
finetune/spider/

# LLM tool-specific context files (kept local, not committed)
CLAUDE.md
AGENTS.md
dev.md
testing.md
release.md
context.yaml
Chinook_PostgreSql.sql
install.sql