name: 'schemalytics_project'
version: '1.0.0'
config-version: 2

profile: 'schemalytics_project'

model-paths: ["models"]
test-paths: ["tests"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  schemalytics_project:
    bronze:
      +materialized: view
    silver:
      +materialized: table
    gold:
      +materialized: table