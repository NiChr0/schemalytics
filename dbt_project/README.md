# schemalytics_project

Generated by Schemalytics.

## Structure
- **bronze/**: Raw passthrough from source (14 models)
- **silver/dimensions/**: Dimensional models (9 models)
- **silver/facts/**: Fact tables (5 models)
- **gold/**: Pre-aggregated metrics (5 models)

## Semantic Layer
See `semantic_layer.yml` for LLM-ready metadata including:
- Available metrics and their definitions
- Dimensional model structure
- Query patterns and guidelines

## Models

### Dimensions
- dim_territories: One row per territorie
- dim_us_states: One row per us_state
- dim_customers: One row per customer
- dim_employees: One row per employee
- dim_shippers: One row per shipper
- dim_categories: One row per categorie
- dim_suppliers: One row per supplier
- dim_region: One row per region
- dim_customer_demographics: One row per customer_demographic

### Facts
- fct_order_details: One row per order_detail
- fct_employee_territories: One row per employee_territorie
- fct_orders: One row per order
- fct_products: One row per product
- fct_customer_customer_demo: One row per customer_customer_demo

### Gold Aggregates
- gold_daily_revenue (daily): Daily revenue and order metrics
- gold_monthly_customer_lifetime_value (monthly): Monthly customer lifetime value
- gold_yearly_product_performance (yearly): Yearly product performance metrics
- gold_daily_inventory_tracking (daily): Daily inventory tracking and sales metrics
- gold_monthly_conversion_optimization (monthly): Monthly conversion optimization metrics

## Usage with LLMs
The semantic layer provides structured metadata for LLM-powered analytics:
1. LLMs can read `semantic_layer.yml` to understand available metrics
2. Pre-aggregated Gold models provide fast query performance
3. Clear grain and dimension definitions help LLMs generate correct queries

Example LLM prompt:
```
Using the semantic layer in semantic_layer.yml, write a SQL query to analyze
daily revenue trends over the last 30 days.
```
